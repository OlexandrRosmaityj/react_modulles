import React from 'react';
import {Outlet} from "react-router-dom";

const AlbomsPage = () => {
    return (
        <div>
            AlbomsPage
            <Outlet/>
        </div>
    );
};

export default AlbomsPage;