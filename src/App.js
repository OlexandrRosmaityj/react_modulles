import {Route, Routes} from "react-router-dom";
import {Layout} from "./components/Layout/Layout";

const App = () => {
    return (
        <div>
<Routes>

</Routes>
        </div>
    );
};

export default App;